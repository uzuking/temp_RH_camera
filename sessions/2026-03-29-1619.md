### セッション記録（自動生成）
- **日付**: 2026-03-29 16:27（16:19初版、16:27更新）
- **ホスト名**: pop-os
- **プロジェクト名**: temp_RH_camera（苗床モニタリングシステム）

- **作業内容・変更概要**:
  - Raspberry Pi 4（Debian 12 Bookworm）のサーバーセットアップ（ステップ10）を実施
  - セキュリティ設定: sudoのNOPASSWD削除、ユーザーパスワード設定
  - SSH有効化、固定IP（192.168.105.200）設定（NetworkManager/nmcli）
  - seedling-serverをPC→RPiにscp転送、uvインストール、動作確認
  - systemdサービス作成（seedling-server.service）で自動起動設定
  - ESP32のcredentials.hのSERVER_HOSTをRPi固定IPに変更、ファームウェア書き込み
  - 画像ビューア用簡易Webサーバー（image-viewer.service, port 9090）をsystemd化
  - セットアップ手順の解説ドキュメント作成（docs/raspberry-pi-setup.md）
  - 設計書のステップ10を完了に更新
  - 本番環境でESP32を独立稼働させ、カメラ画像の定期撮影成功を確認

- **成果や完了事項**:
  - ステップ10（運用準備）完了 — 全10ステップが完了
  - RPiサービス一覧: SSH(22), seedling-server(8080), image-viewer(9090)
  - センサーデータ（10分間隔）のRPiへの定期送信確認済み
  - カメラ画像（1時間間隔）のRPiへの定期送信確認済み
  - reboot後の自動起動確認済み
  - ブラウザで http://192.168.105.200:9090 から画像閲覧可能
  - 本番環境でカメラ定期撮影（1時間間隔）が正常動作確認済み（15:41の画像）
  - RPi電源OFF後も、再電源ONで全サービス自動復帰する構成が完成

- **課題・トラブル**:
  - ESP32起動直後のカメラ撮影でクラッシュ→ブートループ発生
    → 初回カメラ撮影をスキップする修正で解決（定期タスクでは正常動作）
    → 原因は起動直後のUART/メモリ不安定の可能性。電圧問題の可能性も残る
  - scpでPCの.venv（x86_64）がRPi（aarch64）にコピーされてExec format error
    → .venvを削除してuv runで再作成して解決

- **学び・今後のメモ**:
  - Raspberry Pi OS Bookwormのネットワーク管理はNetworkManager（nmcli）
  - `sudo visudo -f` でsudoers.dのファイルを安全に編集できる
  - systemdサービスの作成パターン: service file → daemon-reload → enable --now
  - `python3 -m http.server` でインストール不要の簡易ファイルサーバーが立てられる
  - scp時に.venvは除外すべき（アーキテクチャ依存のバイナリを含む）
  - RPiストレージ19GB、画像は月約14MBなので数年は余裕

- **次回タスク/残TODO**:
  - ESP32起動時カメラクラッシュの根本原因調査（電圧？UART初期化タイミング？）
  - DVC設定（RPi上の画像をDropbox経由でバックアップ）
  - カメラのピント調整確認（最新画像で確認）
  - 長期安定動作の監視（データ欠落がないか）
  - WiFiパワーセーブの確認（RPi側で通信が途切れないか）
